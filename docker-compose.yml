services:
  verso:
    image: ghcr.io/kingkill85/verso:latest
    ports:
      - "3000:3000"
    volumes:
      - verso-data:/data
    env_file: .env
    restart: unless-stopped

volumes:
  verso-data:
